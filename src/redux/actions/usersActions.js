export const LOAD_USERS = state => {
    return {
        type: "LOAD_USERS",
        payload: state
    }
}

export const UPLOAD_USER = state => {
    return {
        type: "UPLOAD_USER",
        payload: state
    }
}